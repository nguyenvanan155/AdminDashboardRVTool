const fs = require('fs'); const path = 'D:/ReviewTool/auto/Admin Dashboard/src/pages/Control.jsx'; let c = fs.readFileSync(path, 'utf8'); c = c.replace('const [selected, setSelected] = useState(allIds);', 'const [selected, setSelected] = useState([]);'); c = c.replace('const depleted = t.limit_reached || t.contentUnused === 0;', 'const depleted = t.limit_reached;'); c = c.replace(/cursor: depleted \? 'not-allowed' : 'pointer'/g, "cursor: 'pointer'"); fs.writeFileSync(path, c, 'utf8'); console.log('Fixed');
